export interface Product {
    code: string,
    providerURL: string,
    provider: string,
    name: string;
    imageURLs: string[];
    description: string;
    brand: string;
    category: string[];
}
